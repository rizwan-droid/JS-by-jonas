/* Variable Mutation and type Corecion
-js automatically converts datatypes called as type corecion.
*/

var firstName = 'john';
var age = 28;
console.log(firstName + ' ' + age);
//here age is a number but while adding it with the string it became a string.

var job, isMarried;
job = 'teacher';
isMarried = false;

console.log(firstName + 'is a' + age + 'years old' + job + '. Is he married?' + isMarried)
// boolean is also converted into string
//undefined can be also be converted


/* Variable Mutation: it is something which means to change the value of a variable */

age = 'twenty eight';
// did not use var keyword as it is already defined
// before it was a number and now it is a string.


//alert() and prompt() function: alert simply shows the content and prompt asks and request to input some data.


